Review all 4 pages in src/ and style.css. Identify any visual inconsistencies across sections and pages — things like fonts not matching the type scale in note.md, spacing that doesn't use --section-gap or --card-gap, colours hardcoded instead of using CSS variables, border-radius values not matching the design system, or component styles that look different from page to page.

Fix every inconsistency found so the whole site looks and feels like one cohesive, polished product. Do not change any content — only fix styling and consistency issues.
