First, review the existing website structure — read CLAUDE.md, note.md, and architecture.md to understand the design system, page layout, and build status. Then go through every HTML file in src/ and identify what content and information each section currently has versus what it still needs.

Research focus areas for The Gate Hot Pot website:
- What content is missing or placeholder in each section
- Whether the current section order on each page serves the commercial goal (drive bookings and walk-ins)
- Whether the design system in note.md is being applied consistently across all files
- Any sections in architecture.md that are marked ⬜ or 🔄 and need attention

Output a clear summary of:
1. Current build status per page (which sections are done, which are missing)
2. Any inconsistencies found between files (CSS variables, naming, structure)
3. One recommended next action to move the build forward
